import 'package:flutter/material.dart';

class AddTokoScreen extends StatelessWidget {
  static const routeName = "/add-toko";

  const AddTokoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
